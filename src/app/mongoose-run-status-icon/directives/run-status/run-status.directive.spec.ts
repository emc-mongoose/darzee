import { RunStatusDirective } from "./run-status.directive";

describe('RunStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new RunStatusDirective();
    expect(directive).toBeTruthy();
  });
});
