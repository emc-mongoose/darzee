export enum FileFormat {
    // NOTE: Make sure to use a text as a key to ...
    // ... to avoid compilation issues at runtime
    JSON = "json", 
    TEXT = "text",
    YML = "yml",
    JS = "js"
}
