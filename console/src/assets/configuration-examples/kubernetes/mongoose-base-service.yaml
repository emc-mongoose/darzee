apiVersion: v1
kind: Service
metadata:
  labels:
    app: mongoose-base
  name: mongoose-base-svc
spec:
  ports:
    # NOTE: Exposting ports
    - name: nmongoose-base-port
      port: 9999
      targetPort: 9999
  selector:
    app: mongoose-base
  type: LoadBalancer
  sessionAffinity: None
